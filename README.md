# Flight Prediction Model

This repository contains a Jupyter notebook `Flight_fare_prediction.ipynb` for exploring and building a flight fare prediction model.

Contents:
- `Flight_fare_prediction.ipynb` — data exploration and model notebook

Instructions:
1. Create a Python environment (e.g., `python -m venv .venv`).
2. Install required packages (add `requirements.txt` if needed).
